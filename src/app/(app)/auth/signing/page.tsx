'use client';

import { Button } from '@/components/ui/button';

const Page = () => {
	const onClick = async () => {};

	return <Button onClick={onClick}>Sign in with Github</Button>;
};

export default Page;
